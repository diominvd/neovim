return {
	"folke/snacks.nvim",
	main = "snacks",
	opts = {
		-- Only the two visual helpers are enabled; the rest of snacks stays disabled.
		indent = { enabled = true },
		scroll = { enabled = true },
	},
}
